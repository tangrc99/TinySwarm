syntax = "proto3";

message MessagePack{  // 客户端所发送的数据包格式
  bytes data = 1;
  string Method = 2;    // 客户端所调用的方法名称
  string Service = 3;    // 客户端所调用的服务名称
  uint32 sequence = 4; // 异步请求需要对数据包进行编号
}

message EchoPack {
  bytes data = 1;   // 服务端所返回的数据
  uint32 sequence = 4;  // 客户端发送数据包的编号
}